
1.高层模块不应该依赖低层模块，两个都应该依赖抽象(接口/抽象类)
	由第三者接口/抽象类来充当高层模块和低层模块的桥梁，这样无论高层或是低层换了，都没事，大家都是用同一个接口。
2.抽象不应该依赖细节，细节应该依赖抽象

就是针对接口编程，不要对实现编程

例如:主板,CPU，内存，硬盘，都是针对接口设计的，如果针对实现来设计，内存条就要对应到具体的某个品牌的主板，
	那就会出现内存需要把主板也换了的尴尬